#include<iostream>
using namespace std;
int main(){
    int x; //making a container name x.
    x=10; 
    int y = 90; //in the container named y, we store the value 90
    cout<<x+y; //we are using the value stored in x and adding 10 to it
}