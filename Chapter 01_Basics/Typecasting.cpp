#include<iostream>
using namespace std;
int main() {
    int x;
    cout<<"Enter a Number: ";
    cin>>x;
    float y = (float)x; //typecasting it means in which type we need output, just put paranthesis onto it
    cout<<y/2;
}